"""OnGarde scanner package.

Provides the security scan pipeline: regex fast path (E-002),
Presidio NLP (E-003), and the scan_or_block atomic wrapper (E-002).
This package is populated across E-002, E-003, and E-010 (Lite mode).
"""
