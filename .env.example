# OnGarde Environment Variables
# --------------------------------
# Primary configuration lives in .ongarde/config.yaml (host, port, upstream URL, etc.)
# These environment variables are optional overrides. A .env file is supported but not required.
# Copy this file to .env and set the values you need.

# ── Authentication ───────────────────────────────────────────────────────────

# Require a valid API key on every request (strongly recommended for production).
# Set to false only for local development / testing.
ONGARDE_AUTH_REQUIRED=true

# ── Server ───────────────────────────────────────────────────────────────────

# Override the port OnGarde listens on. Default: 4242.
# The host/upstream URL are set in .ongarde/config.yaml, not here.
# ONGARDE_PORT=4242

# ── Config Path (optional) ───────────────────────────────────────────────────

# Explicit path to config.yaml. Leave blank to use auto-detection (~/.ongarde/config.yaml).
# ONGARDE_CONFIG=

# ── Developer Options ─────────────────────────────────────────────────────────

# Enable Swagger UI and hot-reload. Dev only — never use in production.
# DEBUG=false

# ── Supabase Audit Backend (optional) ────────────────────────────────────────

# Only required if you are using the Supabase audit backend.
# Leave blank (or omit) when using the default local SQLite audit store.
# SUPABASE_URL=
# SUPABASE_KEY=
